"use client";
import { MessageCardProps } from "./type";
import { Button } from "@/components/ui/button";
import { Avatar, AvatarFallback, AvatarImage } from "@/components/ui/avatar";
export function MessageCard(props: MessageCardProps) {
  return (
    <Button variant="outline" className="h-fit border-2 border-primary-300 p-0">
      <div className="flex h-full w-full flex-row justify-between gap-2 p-2">
        <div className="flex w-fit flex-1 flex-row">
          <Avatar>
            <AvatarImage src="https://github.com/shadcn.png" />
            <AvatarFallback>{props.anotherUserID}</AvatarFallback>
          </Avatar>
          <div className="w-fit">
            <div>{props.senderName}</div>
            <div className="small"> Sticker like</div>
          </div>
        </div>
        <div className="flex flex-none flex-col">
          <div className="h5">23 min</div>

          <div className="m-0 h-5 w-5 rounded-full bg-primary-900 p-0 text-white">
            {props.MessageCardCount}
          </div>
        </div>
      </div>
    </Button>
  );
}
